import Vue from 'vue'
import VueRouter from 'vue-router'

// 加载全局样式
import '../css/index.css'

Vue.use(VueRouter)

const routes = [
  // 配置路由表
]

const router = new VueRouter({
  routes
})

export default router
